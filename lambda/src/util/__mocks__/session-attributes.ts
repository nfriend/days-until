export const getSessionAttributes = (): any => {
  /* noop: this function's return value should be mocked in tests. */
};

export const setSessionAttributes = (): any => {
  /* noop: same as above. */
};
